# DATA PULSE · Social Engine Revival (Aaruush '26 Data Vortex, Round 4)
Streamlit dashboard: Intake (R1-P1) · SQL Core (R1-P2) · Semantic Layer (R2) · Live Monitor (R3).
Run: `pip install -r requirements.txt && streamlit run app.py`
Deploy: push to GitHub → share.streamlit.io → main file `app.py`.
`prep.py` rebuilds `data/` and scored Round 3 output from the original files (put them in `raw/`).
